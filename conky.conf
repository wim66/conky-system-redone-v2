--[[
conky-system (all-Lua rebuild)
by @wim66 (with Claude)
3-July-2026
]]

conky.config = {
    alignment = 'top_left',
    gap_x = 30,
    gap_y = 0, -- widget.lua's own CFG.vertical_align now does the real
                 -- vertical positioning; left here only because it's
                 -- harmless, not because it has any visible effect.
    minimum_width = 280,
    minimum_height = 1040,
    maximum_width = 280,

    use_xft = true,
    override_utf8_locale = true,

    no_buffers = true,
    double_buffer = true,
    update_interval = 1,
    total_run_times = 0,
    cpu_avg_samples = 2,
    net_avg_samples = 2,

    background = true,
    own_window = true,
    own_window_type = 'normal',
    own_window_class = 'Conky',
    own_window_title = 'conky-system',
    own_window_argb_visual = true,
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',

    lua_load = './widget.lua',
    lua_draw_hook_post = 'main',
};

conky.text = [[

]]